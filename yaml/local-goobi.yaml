rest:
    urlBase: http://arche-local
    pathBase: /api/
doorkeeper:
    epicPid:
        pswd:
    certVerify: false
resolver:
    repositories:
        self:
            baseUrl: http://arche-local/api/

